# 시연 치트시트 (영상 촬영용)

검증 일자: 2026-06-11 · 로컬 기준 (backend: localhost:8000, frontend: localhost:5173)

## 준비

```bash
# 백엔드
cd backend && uvicorn app.main:app --port 8000

# 프론트엔드
cd frontend && npm run dev

# 날씨 전환 (화면에 안 보임 — 촬영 중 터미널에서 조작)
./demo.sh uv_high   # 자외선 높음 (UV 7, 한낮)
./demo.sh night     # 야간 (23시)
./demo.sh rain      # 비 (강수 85%)
./demo.sh off       # 실제 날씨 복귀
```

> 프리셋을 바꾼 뒤에는 반드시 **"새 탐색"** 후 출발·도착을 다시 찍어야 새 경로가 계산된다.

## 촬영 직전 체크

1. 백엔드를 재시작했다면 **데모 프리셋이 초기화**된다 — 각 장면 직전에 `./demo.sh <preset>` 재실행
2. `./demo.sh` (인자 없이)로 현재 프리셋 확인
3. 프리셋 변경 → "새 탐색" → 출발·도착 클릭 순서 엄수
4. 브라우저는 `localhost:5173` (배포 주소 아님 — 배포는 push·재배포 후에만 최신)

## 추천 시나리오 (실측 검증된 수치)

### ★ 메인: 같은 구간, 날씨 따라 경로가 뒤집힘 — 시청 → 시립미술관

- 출발 클릭: `36.3505, 127.3845` (대전시청)
- 도착 클릭: `36.3656, 127.3850` (시립미술관/예술의전당)

| 프리셋 | 경로 | 수치 |
|---|---|---|
| `uv_high` | **한밭수목원 관통** (지도에서 녹지를 가로지르는 게 보임) | 그늘 67% vs 최단 41% |
| `night` | **어두운 수목원을 피해** 밝은 대로로 우회 | 밝음 72% vs 최단 32% |

스토리: "같은 목적지인데, 낮엔 그늘을 찾아 공원으로 들어가고 밤엔 공원을 피해 가로등 길로 나옵니다."

### 보조 1: 자외선 — 시청 → 정부청사역 (그늘 효과 최대)

- 도착 클릭: `36.3590, 127.3815`
- `uv_high`: 그늘 **68% vs 34% (두 배)**, 거리 손해 200m뿐 (1.6km vs 1.4km)

### 보조 2: 비 — 대전역 → 으능정이거리 (지하상가 경유)

- 출발 클릭: `36.3315, 127.4346` (대전역) / 도착 클릭: `36.3284, 127.4282`
- `rain`: 차폐 **72% vs 22%** — 경로가 **중앙로 지하상가를 실제로 통과** (중앙로 선형을 따라가는 주황 선)
- 나레이션 포인트: "비가 오면 지하상가를 경유하는 실내 경로로 바뀝니다"

### 커스텀 경로 (순환 코스)

경로 그리기에서 출발점 근처로 돌아오는 한 바퀴 코스를 클릭
→ 점선 직선 미리보기가 **실제 도로를 따라가는 실선**으로 바뀌는 장면이 포인트.

## 피해야 할 구간

- 시청 → 중촌동(신도칼국수) 방향: 실데이터상 그늘길이 없어 차이가 +12%p에 그침.
  이 경우 카드가 "이 방향은 그늘이 적습니다 — 모자·선크림 필수"로 정직 표기되는데,
  이것 자체를 "과장하지 않는 추천"으로 짧게 보여주는 것도 방법.

## 발표용 한 줄 차별점

> 최단 경로가 아니라 "지금 날씨에 최선인 경로"를 찾는다 — 판단 근거는 추정이 아니라
> 실측 데이터(OSM 가로수 300구간·공원 226개·하천 보정·지하차도 제외).
